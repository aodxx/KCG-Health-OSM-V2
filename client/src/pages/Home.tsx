import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { Activity, ArrowUpRight, Bell, BookOpen, CalendarDays, Check, ChevronRight, CircleHelp, ClipboardCheck, ClipboardList, Clock3, FileDown, Home as HomeIcon, LayoutDashboard, LogIn, MapPin, Menu, Phone, Plus, Search, ShieldCheck, Siren, Sparkles, Users, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { toast } from "sonner";
import { apiConfig, apiRequest } from "@/lib/api";

type Task = { taskId?: string; taskCode?: string; title?: string; personName?: string; personId?: string; householdId?: string; areaId?: string; taskType?: string; priority?: string; status?: string; dueAt?: string; instruction?: string; person?: string; place?: string; type?: string; due?: string };
type Person = { personId?: string; citizenCode?: string; firstName?: string; lastName?: string; householdId?: string; phone?: string; residencyStatus?: string };
type RiskFlag = { riskFlagId?: string; riskCode?: string; suggestedLevel?: string; confirmedLevel?: string; status?: string; reason?: string; detectedAt?: string; personId?: string };
type DashboardData = { metrics?: { today?: number; overdue?: number; completed?: number; riskOpen?: number }; tasks?: Task[]; risks?: RiskFlag[]; user?: { userId?: string; displayName?: string; primaryRole?: string; status?: string } };
type GoogleCredential = { credential: string };
type GoogleIdentity = { accounts: { id: { initialize: (config: { client_id: string; callback: (response: GoogleCredential) => void }) => void; renderButton: (parent: HTMLElement, options: { theme: string; size: string; width: number }) => void; prompt: () => void } } };

const navItems = [{ label: "ภาพรวม", icon: LayoutDashboard }, { label: "งานเยี่ยมบ้าน", icon: ClipboardList }, { label: "ทะเบียนชุมชน", icon: Users }, { label: "รายงาน", icon: FileDown }];

function StatusPill({ children, tone = "blue" }: { children: React.ReactNode; tone?: "blue" | "coral" | "yellow" | "green" }) {
  const styles = { blue: "bg-[#e4eff8] text-[#235476]", coral: "bg-[#f9e5df] text-[#a94835]", yellow: "bg-[#fff1bd] text-[#775a10]", green: "bg-[#e4f0e6] text-[#2d6846]" };
  return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-semibold ${styles[tone]}`}><span className="h-1.5 w-1.5 rounded-full bg-current" />{children}</span>;
}

function Metric({ label, value, note, accent, icon: Icon }: { label: string; value: string; note: string; accent: string; icon: React.ElementType }) {
  return <div className="metric-card"><div className="flex items-start justify-between"><span className="section-kicker">{label}</span><span className={`metric-icon ${accent}`}><Icon size={17} /></span></div><div className="mt-5 flex items-end gap-2"><span className="font-display text-[2.35rem] leading-none text-[#19364b]">{value}</span><span className="mb-0.5 text-xs font-medium text-[#5d7788]">{note}</span></div></div>;
}

export default function Home() {
  const [publicMode, setPublicMode] = useState(false);
  const [menuOpen, setMenuOpen] = useState(false);
  const [activeNav, setActiveNav] = useState("ภาพรวม");
  const [view, setView] = useState<"dashboard" | "visit">("dashboard");
  const [search, setSearch] = useState("");
  const [registryQuery, setRegistryQuery] = useState("");
  const [registryResults, setRegistryResults] = useState<Person[]>([]);
  const [registryLoading, setRegistryLoading] = useState(false);
  const [registryError, setRegistryError] = useState<string | null>(null);
  const [dashboard, setDashboard] = useState<DashboardData | null>(null);
  const [selectedTask, setSelectedTask] = useState<Task | undefined>();
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [loggedIn, setLoggedIn] = useState(() => Boolean(sessionStorage.getItem("kcg_session_token")));

  const loadDashboard = useCallback(async () => {
    if (!loggedIn) return;
    setLoading(true); setError(null);
    try { const [summary, taskList] = await Promise.all([apiRequest<DashboardData>("dashboard"), apiRequest<{ items: Task[] }>("tasks.list")]); setDashboard({ ...(summary.data || {}), tasks: taskList.data?.items || summary.data?.tasks || [] }); }
    catch (err) { setError(err instanceof Error ? err.message : "ไม่สามารถโหลด Dashboard ได้"); }
    finally { setLoading(false); }
  }, [loggedIn]);

  useEffect(() => { void loadDashboard(); }, [loadDashboard]);

  const searchRegistry = useCallback(async () => {
    const query = registryQuery.trim();
    if (!query) { setRegistryResults([]); return; }
    setRegistryLoading(true); setRegistryError(null);
    try { const result = await apiRequest<{ items: Person[] }>("registry.search", { query }); setRegistryResults(result.data?.items || []); }
    catch (err) { setRegistryError(err instanceof Error ? err.message : "ค้นหาทะเบียนไม่สำเร็จ"); }
    finally { setRegistryLoading(false); }
  }, [registryQuery]);

  const handleGoogleCredential = useCallback(async ({ credential }: GoogleCredential) => {
    try { setLoading(true); const response = await apiRequest<{ sessionToken: string }>("auth.google", { idToken: credential, deviceLabel: navigator.userAgent.slice(0, 80) }); if (response.data?.sessionToken) { sessionStorage.setItem("kcg_session_token", response.data.sessionToken); setLoggedIn(true); toast.success("เข้าสู่ระบบแล้ว"); } }
    catch (err) { toast.error(err instanceof Error ? err.message : "เข้าสู่ระบบไม่สำเร็จ"); }
    finally { setLoading(false); }
  }, []);

  const login = () => {
    if (!apiConfig.configured) { toast.error("ยังไม่ได้ตั้งค่า API"); return; }
    const googleIdentity = (window as unknown as { google?: GoogleIdentity }).google;
    const clientId = import.meta.env.VITE_GOOGLE_CLIENT_ID as string | undefined;
    if (!googleIdentity) { toast.error("ยังโหลด Google Sign-In ไม่เสร็จ", { description: "กรุณารีเฟรชหน้าแล้วลองใหม่" }); return; }
    if (!clientId) { toast.error("ยังไม่ได้ตั้งค่า Google Client ID", { description: "เพิ่ม VITE_GOOGLE_CLIENT_ID ใน environment ของ frontend" }); return; }
    googleIdentity.accounts.id.initialize({ client_id: clientId, callback: handleGoogleCredential });
    googleIdentity.accounts.id.prompt();
  };

  const openTask = async (task: Task) => { const taskId = task.taskId || task.taskCode; try { if (taskId) await apiRequest("tasks.updateStatus", { taskId, status: "in_progress" }); setSelectedTask(task); setView("visit"); } catch (err) { toast.error(err instanceof Error ? err.message : "เปิดงานไม่สำเร็จ"); } };
  const logout = async () => { try { await apiRequest("auth.logout"); } catch { /* session may already be expired */ } sessionStorage.removeItem("kcg_session_token"); setLoggedIn(false); setDashboard(null); setView("dashboard"); toast.success("ออกจากระบบแล้ว"); };

  if (publicMode) return <PublicPortal onBack={() => setPublicMode(false)} />;
  if (!loggedIn) return <StaffLogin loading={loading} onLogin={login} onPublic={() => setPublicMode(true)} onCredential={handleGoogleCredential} />;
  if (view === "visit") return <VisitForm task={selectedTask} onBack={() => setView("dashboard")} onSaved={() => { setView("dashboard"); void loadDashboard(); }} />;
  if (activeNav === "ทะเบียนชุมชน") return <RegistryView query={registryQuery} results={registryResults} loading={registryLoading} error={registryError} onQuery={setRegistryQuery} onSearch={() => void searchRegistry()} onBack={() => setActiveNav("ภาพรวม")} />;
  if (activeNav === "รายงาน") return <ReportsView dashboard={dashboard} onBack={() => setActiveNav("ภาพรวม")} />;

  const tasks = dashboard?.tasks || [];
  const filteredTasks = useMemo(() => tasks.filter((task) => JSON.stringify(task).toLowerCase().includes(search.toLowerCase())), [tasks, search]);
  const metrics = dashboard?.metrics || {};
  return <div className="app-shell">
    <aside className={`sidebar ${menuOpen ? "sidebar-open" : ""}`}>
      <Brand />
      <div className="sidebar-context"><span className="context-dot" /> โคกชะงาย · พัทลุง</div>
      <nav className="nav-list" aria-label="เมนูหลัก">{navItems.map(({ label, icon: Icon }) => <button key={label} className={`nav-item ${activeNav === label ? "active" : ""}`} onClick={() => { setActiveNav(label); setMenuOpen(false); if (label === "งานเยี่ยมบ้าน") { setView("dashboard"); } else if (label === "ทะเบียนชุมชน" || label === "รายงาน") { setView("dashboard"); } else if (label !== "ภาพรวม") toast.info(`${label} จะเชื่อมต่อในขั้นถัดไป`); }}><Icon size={18} /><span>{label}</span>{label === "งานเยี่ยมบ้าน" && <span className="nav-count">{tasks.length}</span>}</button>)}</nav>
      <div className="sidebar-bottom"><div className="safety-note"><ShieldCheck size={17} /><div><strong>ข้อมูลอยู่ในขอบเขต</strong><span>แสดงตามสิทธิ์พื้นที่ของคุณ</span></div></div><button className="public-link" onClick={() => setPublicMode(true)}><HomeIcon size={16} /> ดูหน้าสำหรับประชาชน <ArrowUpRight size={14} /></button><div className="profile-row"><div className="avatar">อ</div><div className="min-w-0"><div className="profile-name">{dashboard?.user?.displayName || "ผู้ใช้งานระบบ"}</div><div className="profile-role">{dashboard?.user?.primaryRole || "อสม."} · พื้นที่รับผิดชอบ</div></div><button className="icon-button" aria-label="ออกจากระบบ" onClick={logout}><LogIn size={17} /></button></div></div>
    </aside>
    {menuOpen && <button className="sidebar-scrim" onClick={() => setMenuOpen(false)} aria-label="ปิดเมนู" />}
    <main className="main-panel"><header className="topbar"><button className="mobile-menu icon-button" onClick={() => setMenuOpen(true)} aria-label="เปิดเมนู"><Menu size={21} /></button><div className="breadcrumb"><span>งานของฉัน</span><ChevronRight size={14} /><strong>{activeNav}</strong></div><div className="top-actions"><button className="icon-button" aria-label="การแจ้งเตือน"><Bell size={19} /></button><button className="user-chip"><span className="avatar small">อ</span><span className="hidden sm:block">{dashboard?.user?.displayName || "ผู้ใช้งาน"}</span></button></div></header>
      <div className="content-wrap"><section className="welcome-row"><div><div className="eyebrow"><span className="sun-line" /> Dashboard จากข้อมูลจริง</div><h1>สวัสดีครับ <span className="wave-mark">/</span></h1><p>ข้อมูลถูกโหลดจาก Google Apps Script · Staging</p></div><div className="quick-actions"><Button className="primary-button" onClick={() => { setSelectedTask(undefined); setView("visit"); }}><Plus size={17} /> บันทึกเยี่ยมบ้าน</Button><button className="secondary-action" onClick={() => void loadDashboard()}>{loading ? "กำลังโหลด..." : "รีเฟรชข้อมูล"} <Activity size={15} /></button></div></section>
      <div className="notice-bar"><div className="notice-icon"><Sparkles size={16} /></div><div><strong>{error ? "เชื่อมต่อข้อมูลไม่สำเร็จ" : "เชื่อมต่อข้อมูลจริงแล้ว"}</strong><span>{error || `มี ${tasks.length} งานในขอบเขตของคุณ`}</span></div><button onClick={() => void loadDashboard()}>ลองใหม่ <ArrowUpRight size={15} /></button></div>
      <section className="metrics-grid"><Metric label="งานวันนี้" value={String(metrics.today ?? 0)} note="รายการ" accent="metric-yellow" icon={ClipboardCheck} /><Metric label="งานค้าง" value={String(metrics.overdue ?? 0)} note="ต้องติดตาม" accent="metric-coral" icon={Clock3} /><Metric label="เคสเสี่ยง" value={String(metrics.riskOpen ?? 0)} note="รอติดตาม" accent="metric-blue" icon={Siren} /><Metric label="โหมดระบบ" value="จริง" note="Staging" accent="metric-green" icon={ShieldCheck} /></section>
      <div className="section-grid"><section id="task-list" className="task-panel"><div className="panel-heading"><div><div className="section-kicker">คิวปฏิบัติการ</div><h2>งานที่ต้องทำ</h2></div><button className="text-action" onClick={() => void loadDashboard()}>โหลดใหม่ <ArrowUpRight size={15} /></button></div><div className="search-box"><Search size={17} /><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="ค้นหาชื่อ บ้าน หรือประเภทงาน" /></div><div className="task-list">{loading && <div className="empty-state">กำลังโหลดข้อมูลจาก Apps Script...</div>}{!loading && filteredTasks.map((task) => <TaskRow key={task.taskId || task.taskCode} task={task} onOpen={() => void openTask(task)} />)}{!loading && filteredTasks.length === 0 && <div className="empty-state">ไม่พบงานในขอบเขตของคุณ</div>}</div></section><aside className="activity-panel"><div className="panel-heading"><div><div className="section-kicker">สถานะการเชื่อมต่อ</div><h2>ระบบจริง</h2></div><Check className="text-[#2d6846]" size={18} /></div><div className="timeline"><div className="timeline-item"><div className="timeline-dot success" /><div><div className="timeline-time">API</div><h3>Google Apps Script</h3><p>เชื่อมต่อ endpoint สำเร็จ</p></div></div><div className="timeline-item"><div className="timeline-dot blue" /><div><div className="timeline-time">Data</div><h3>Google Sheets</h3><p>อ่านข้อมูลตาม session และพื้นที่</p></div></div></div><div className="mt-6 border-t border-[#e6ecec] pt-5"><div className="section-kicker">คิวความเสี่ยง</div><div className="mt-3 space-y-2">{(dashboard?.risks || []).slice(0, 3).map((risk) => <div key={risk.riskFlagId || `${risk.personId}-${risk.riskCode}`} className="flex items-center justify-between rounded-lg bg-[#fff7ed] px-3 py-2 text-xs"><span className="font-semibold text-[#9c5d24]">{risk.riskCode || "Risk Flag"}</span><span className="text-[#8a6d51]">{risk.status || "pending_review"}</span></div>)}{!(dashboard?.risks || []).length && <p className="text-xs text-[#78909a]">ยังไม่มีเคสเสี่ยงในขอบเขต</p>}</div></div><div className="mini-card"><div className="mini-card-icon"><CalendarDays size={18} /></div><div><strong>สร้างบันทึกเยี่ยมบ้าน</strong><span>ส่งผ่าน visits.create</span></div><button onClick={() => setView("visit")}><ChevronRight size={17} /></button></div></aside></div>
      <section className="bottom-strip"><div className="strip-copy"><div className="section-kicker">การบันทึกที่ปลอดภัย</div><h2>ทุกการเยี่ยมบ้านมีร่องรอยให้ตรวจสอบ</h2><p>ข้อมูลจะถูกตรวจสิทธิ์พื้นที่และบันทึก Audit Log ก่อนส่งต่อเจ้าหน้าที่</p></div><div className="strip-stat"><span className="strip-number">API</span><span>requestId<br />ทุกคำขอ</span></div><div className="strip-mark"><ShieldCheck size={28} /><span>Privacy<br />by design</span></div></section><footer className="footer"><span>KCG Health OSM · เชื่อมต่อ Staging API</span><span><button onClick={logout}>ออกจากระบบ</button><span className="footer-sep">/</span><button onClick={() => toast.info("ใช้ข้อมูลสังเคราะห์เท่านั้นใน Staging")}>นโยบายข้อมูล</button></span></footer></div>
    </main></div>;
}

function TaskRow({ task, onOpen }: { task: Task; onOpen: () => void }) { const id = task.taskId || task.taskCode || "-"; const person = task.personName || task.person || task.title || "ไม่ระบุชื่อ"; const place = task.place || task.areaId || task.householdId || "ไม่ระบุพื้นที่"; const type = task.taskType || task.type || "เยี่ยมบ้าน"; const due = task.due || (task.dueAt ? new Date(task.dueAt).toLocaleString("th-TH", { dateStyle: "short", timeStyle: "short" }) : "ยังไม่กำหนด"); const urgent = task.priority === "urgent" || task.priority === "เร่งด่วน"; return <div className="task-row"><div className={`task-priority ${urgent ? "coral" : "blue"}`} /><div className="task-main"><div className="flex flex-wrap items-center gap-2"><span className="task-code">{id}</span><StatusPill tone={urgent ? "coral" : "blue"}>{task.status || (urgent ? "เร่งด่วน" : "รอดำเนินการ")}</StatusPill></div><h3>{person}</h3><p>{type} <span>·</span> {place}</p></div><div className="task-due"><span>{due}</span><button className="task-arrow" onClick={onOpen} aria-label={`เปิดแบบฟอร์ม ${person}`}><ChevronRight size={18} /></button></div></div>; }

function StaffLogin({ loading, onLogin, onPublic, onCredential }: { loading: boolean; onLogin: () => void; onPublic: () => void; onCredential: (response: GoogleCredential) => void }) {
  const googleButtonRef = useRef<HTMLDivElement>(null);
  const clientId = import.meta.env.VITE_GOOGLE_CLIENT_ID as string | undefined;

  useEffect(() => {
    let attempts = 0;
    let timer: number | undefined;
    const render = () => {
      const googleIdentity = (window as unknown as { google?: GoogleIdentity }).google;
      const parent = googleButtonRef.current;
      if (googleIdentity && clientId && parent) {
        googleIdentity.accounts.id.initialize({ client_id: clientId, callback: onCredential });
        parent.replaceChildren();
        googleIdentity.accounts.id.renderButton(parent, { theme: "outline", size: "large", width: 320 });
        return;
      }
      if (attempts++ < 30) timer = window.setTimeout(render, 200);
    };
    render();
    return () => { if (timer) window.clearTimeout(timer); };
  }, [clientId, onCredential]);

  return <div className="public-page"><header className="public-topbar"><Brand /><button className="public-back" onClick={onPublic}><HomeIcon size={16} /> หน้าสำหรับประชาชน</button></header><main className="public-content"><section className="public-hero"><div className="public-hero-copy"><div className="eyebrow"><span className="sun-line" /> KCG Health OSM · Staging</div><h1>เข้าสู่ระบบ<br /><em>งานสุขภาพชุมชน</em></h1><p>ใช้บัญชี Google ที่ได้รับอนุญาตเพื่อเปิด Dashboard และบันทึกเยี่ยมบ้านตามพื้นที่รับผิดชอบ</p><div ref={googleButtonRef} className="mt-6 min-h-[40px]" aria-label="เข้าสู่ระบบด้วย Google" /> <Button className="primary-button mt-3" onClick={onLogin} disabled={loading}><LogIn size={17} /> {loading ? "กำลังตรวจสอบ..." : "เปิดหน้าต่างเข้าสู่ระบบอีกครั้ง"}</Button><p className="mt-4 text-xs text-[#6f8792]">ระบบจะไม่เปิดทะเบียนหรือข้อมูลสุขภาพจนกว่าจะตรวจสอบสิทธิ์สำเร็จ</p></div><div className="hero-image-wrap"><img src="/manus-storage/kcg-community-hero_d89be46d.jpg" alt="เจ้าหน้าที่และ อสม. ในชุมชน" /></div></section></main></div>;
}

function VisitForm({ task, onBack, onSaved }: { task?: Task; onBack: () => void; onSaved: () => void }) { const [form, setForm] = useState({ personId: task?.personId || "", householdId: task?.householdId || "", areaId: task?.areaId || "", visitType: task?.taskType || "home_visit", symptomSummary: "", observation: "", actionTaken: "", weightKg: "", heightCm: "" }); const [review, setReview] = useState(false); const [saving, setSaving] = useState(false); const [saved, setSaved] = useState<{ visitId?: string; bmi?: number | null }>(); const [error, setError] = useState<string | null>(null); const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value })); const submit = async () => { setSaving(true); setError(null); try { const result = await apiRequest<{ visitId: string; bmi: number | null }>("visits.create", { ...form, taskId: task?.taskId || task?.taskCode, clientRecordedAt: new Date().toISOString(), weightKg: form.weightKg ? Number(form.weightKg) : undefined, heightCm: form.heightCm ? Number(form.heightCm) : undefined }); setSaved(result.data); toast.success("บันทึกเยี่ยมบ้านแล้ว", { description: result.data?.visitId }); } catch (err) { setError(err instanceof Error ? err.message : "บันทึกไม่สำเร็จ"); } finally { setSaving(false); } };
  return <div className="public-page"><header className="public-topbar"><Brand /><button className="public-back" onClick={onBack}><X size={16} /> กลับ Dashboard</button></header><main className="content-wrap max-w-5xl mx-auto"><section className="welcome-row"><div><div className="eyebrow"><span className="sun-line" /> แบบฟอร์มเยี่ยมบ้าน · ตรวจสอบก่อนส่ง</div><h1>บันทึกการเยี่ยมบ้าน <span className="wave-mark">/</span></h1><p>{task ? `งาน ${task.taskId || task.taskCode || "ที่เลือก"}` : "บันทึกงานใหม่จากพื้นที่รับผิดชอบ"}</p></div></section>{saved ? <section className="task-panel"><div className="notice-bar"><div className="notice-icon"><Check size={16} /></div><div><strong>บันทึกสำเร็จ</strong><span>Visit ID: {saved.visitId} {saved.bmi ? `· BMI ${saved.bmi}` : ""}</span></div></div><ScreeningPanel visitId={saved.visitId} personId={form.personId} /><Button className="primary-button mt-8" onClick={onSaved}>กลับไป Dashboard</Button></section> : <section className="task-panel"><div className="form-grid"><label>Person ID<Input value={form.personId} onChange={(e) => update("personId", e.target.value)} placeholder="รหัสบุคคล" /></label><label>Household ID<Input value={form.householdId} onChange={(e) => update("householdId", e.target.value)} placeholder="รหัสครัวเรือน" /></label><label>Area ID<Input value={form.areaId} onChange={(e) => update("areaId", e.target.value)} placeholder="รหัสพื้นที่" /></label><label>ประเภทเยี่ยมบ้าน<Input value={form.visitType} onChange={(e) => update("visitType", e.target.value)} /></label><label className="full">สรุปอาการ<Input value={form.symptomSummary} onChange={(e) => update("symptomSummary", e.target.value)} placeholder="กรอกเท่าที่จำเป็น" /></label><label className="full">การสังเกต<textarea value={form.observation} onChange={(e) => update("observation", e.target.value)} placeholder="บันทึกข้อสังเกตจากการเยี่ยมบ้าน" /></label><label className="full">การดำเนินการ<textarea value={form.actionTaken} onChange={(e) => update("actionTaken", e.target.value)} placeholder="คำแนะนำหรือการส่งต่อ" /></label><label>น้ำหนัก (กก.)<Input type="number" value={form.weightKg} onChange={(e) => update("weightKg", e.target.value)} /></label><label>ส่วนสูง (ซม.)<Input type="number" value={form.heightCm} onChange={(e) => update("heightCm", e.target.value)} /></label></div>{error && <div className="mt-5 rounded-lg bg-[#f9e5df] p-3 text-sm text-[#a94835]">{error}</div>}{review ? <div className="mt-8 rounded-xl border border-[#eadfb6] bg-[#fffaf0] p-5"><strong>ตรวจสอบก่อนส่ง</strong><p className="mt-2 text-sm text-[#536d7b]">ข้อมูลนี้จะถูกส่งไปยัง Google Sheets และสร้าง Audit Log คุณยืนยันการบันทึกหรือไม่</p><div className="mt-4 flex gap-3"><Button className="primary-button" onClick={() => void submit()} disabled={saving}>{saving ? "กำลังบันทึก..." : "ยืนยันส่งข้อมูล"}</Button><Button variant="outline" onClick={() => setReview(false)}>กลับไปแก้ไข</Button></div></div> : <div className="mt-8 flex gap-3"><Button className="primary-button" onClick={() => setReview(true)} disabled={!form.personId || !form.householdId || !form.visitType}><Check size={17} /> ตรวจสอบก่อนส่ง</Button><Button variant="outline" onClick={onBack}>ยกเลิก</Button></div>}</section>}</main></div>; }

function ScreeningPanel({ visitId, personId }: { visitId?: string; personId: string }) {
  const [template, setTemplate] = useState<{ templateId?: string; templateVersion?: string; nameTh?: string; schemaJson?: string } | null>(null);
  const [answer, setAnswer] = useState("");
  const [status, setStatus] = useState<"loading" | "ready" | "empty" | "saved" | "error">("loading");
  useEffect(() => { void apiRequest<{ items: Array<{ templateId?: string; templateVersion?: string; nameTh?: string; schemaJson?: string }> }>("screening.templates").then((result) => { const first = result.data?.items?.[0]; setTemplate(first || null); setStatus(first ? "ready" : "empty"); }).catch(() => setStatus("error")); }, []);
  const save = async () => { if (!template || !visitId || !answer.trim()) return; setStatus("loading"); try { await apiRequest("screening.save", { visitId, personId, templateId: template.templateId, templateVersion: template.templateVersion, responses: [{ questionCode: "free_text_review", answerType: "text", answerValue: answer.trim() }] }); setStatus("saved"); } catch { setStatus("error"); } };
  if (status === "loading") return <div className="mt-6 rounded-xl border border-[#e4ecee] bg-[#f8fbfb] p-4 text-sm text-[#607986]">กำลังโหลดแบบคัดกรองที่หน่วยงานอนุมัติ...</div>;
  if (status === "empty") return <div className="mt-6 rounded-xl border border-dashed border-[#d7e1e3] bg-[#fbfcfb] p-4 text-sm text-[#718793]">ยังไม่มี Screening Template ที่เผยแพร่และอนุมัติใน Staging จึงยังไม่แสดงคำถามทางการแพทย์</div>;
  if (status === "saved") return <div className="mt-6 rounded-xl border border-[#bddbc6] bg-[#f3fbf5] p-4 text-sm text-[#2d6846]">บันทึกคำตอบคัดกรองแล้ว และเชื่อมกับ Visit ID นี้เรียบร้อย</div>;
  return <div className="mt-6 rounded-xl border border-[#e4ecee] bg-[#f8fbfb] p-4"><div className="section-kicker">แบบคัดกรองที่อนุมัติ</div><h3 className="mt-1 font-semibold text-[#19364b]">{template?.nameTh || "แบบคัดกรอง"}</h3><p className="mt-2 text-xs text-[#6e8590]">เวอร์ชัน {template?.templateVersion || "-"} · บันทึกเป็น Screening Response ที่ตรวจสอบย้อนหลังได้</p><textarea className="mt-4 w-full rounded-lg border border-[#d8e3e5] bg-white p-3 text-sm" rows={3} value={answer} onChange={(event) => setAnswer(event.target.value)} placeholder="บันทึกคำตอบตามแบบที่หน่วยงานอนุมัติ" /><div className="mt-3 flex items-center gap-3"><Button className="primary-button" onClick={() => void save()} disabled={!answer.trim()}>บันทึกคำตอบคัดกรอง</Button>{status === "error" && <span className="text-xs text-[#a94835]">บันทึกไม่สำเร็จ กรุณาลองใหม่</span>}</div></div>;
}

function RegistryView({ query, results, loading, error, onQuery, onSearch, onBack }: { query: string; results: Person[]; loading: boolean; error: string | null; onQuery: (value: string) => void; onSearch: () => void; onBack: () => void }) {
  return <div className="app-shell"><aside className="sidebar"><Brand /><div className="sidebar-context"><span className="context-dot" /> โคกชะงาย · พัทลุง</div><div className="sidebar-bottom"><div className="safety-note"><ShieldCheck size={17} /><div><strong>ข้อมูลอยู่ในขอบเขต</strong><span>แสดงตามสิทธิ์พื้นที่ของคุณ</span></div></div><button className="public-link" onClick={onBack}><LayoutDashboard size={16} /> กลับภาพรวม</button></div></aside><main className="main-panel"><header className="topbar"><div className="breadcrumb"><span>งานของฉัน</span><ChevronRight size={14} /><strong>ทะเบียนชุมชน</strong></div></header><div className="content-wrap"><section className="welcome-row"><div><div className="eyebrow"><span className="sun-line" /> ทะเบียนชุมชน · ค้นหาตามสิทธิ์</div><h1>ค้นหาบุคคล <span className="wave-mark">/</span></h1><p>แสดงข้อมูลเท่าที่จำเป็นและปกปิดเบอร์ตามสิทธิ์</p></div></section><section className="task-panel"><div className="search-box"><Search size={17} /><Input value={query} onChange={(e) => onQuery(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") onSearch(); }} placeholder="ค้นชื่อ รหัสบุคคล หรือรหัสครัวเรือน" /><Button className="primary-button" onClick={onSearch} disabled={loading}>{loading ? "กำลังค้นหา..." : "ค้นหา"}</Button></div>{error && <div className="mt-5 rounded-lg bg-[#f9e5df] p-3 text-sm text-[#a94835]">{error}</div>}<div className="mt-6 space-y-2">{results.map((person) => <div key={person.personId} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[#e4ecee] bg-white p-4"><div><div className="text-xs font-semibold text-[#7a929d]">{person.citizenCode || person.personId}</div><h3 className="mt-1 font-semibold text-[#19364b]">{person.firstName || "ไม่ระบุชื่อ"} {person.lastName || ""}</h3><p className="mt-1 text-xs text-[#66808d]">ครัวเรือน {person.householdId || "-"} · โทร {person.phone || "ไม่แสดง"}</p></div><StatusPill tone="blue">{person.residencyStatus || "active"}</StatusPill></div>)}{!loading && !results.length && <div className="empty-state">กรอกคำค้นเพื่อค้นหาบุคคลในขอบเขตของคุณ</div>}</div></section></div></main></div>;
}

function ReportsView({ dashboard, onBack }: { dashboard: DashboardData | null; onBack: () => void }) {
  const metrics = dashboard?.metrics || {};
  return <div className="app-shell"><aside className="sidebar"><Brand /><div className="sidebar-context"><span className="context-dot" /> โคกชะงาย · พัทลุง</div><div className="sidebar-bottom"><div className="safety-note"><ShieldCheck size={17} /><div><strong>ข้อมูลอยู่ในขอบเขต</strong><span>รายงานสรุปตามสิทธิ์</span></div></div><button className="public-link" onClick={onBack}><LayoutDashboard size={16} /> กลับภาพรวม</button></div></aside><main className="main-panel"><header className="topbar"><div className="breadcrumb"><span>งานของฉัน</span><ChevronRight size={14} /><strong>รายงาน</strong></div></header><div className="content-wrap"><section className="welcome-row"><div><div className="eyebrow"><span className="sun-line" /> รายงานสรุป · Staging</div><h1>ภาพรวมการปฏิบัติงาน <span className="wave-mark">/</span></h1><p>ข้อมูลสรุปจาก Dashboard ตามพื้นที่ที่ได้รับสิทธิ์</p></div></section><section className="metrics-grid"><Metric label="งานวันนี้" value={String(metrics.today ?? 0)} note="รายการ" accent="metric-yellow" icon={ClipboardCheck} /><Metric label="งานค้าง" value={String(metrics.overdue ?? 0)} note="รายการ" accent="metric-coral" icon={Clock3} /><Metric label="เคสเสี่ยง" value={String(metrics.riskOpen ?? 0)} note="รายการ" accent="metric-blue" icon={Siren} /><Metric label="ส่งแล้ว" value={String(metrics.completed ?? 0)} note="รายการ" accent="metric-green" icon={Activity} /></section><section className="task-panel mt-6"><div className="panel-heading"><div><div className="section-kicker">การส่งออก</div><h2>เตรียมรายงาน</h2></div><Badge className="bg-[#fff1bd] text-[#775a10]">P0 · ตาม PRD</Badge></div><p className="mt-3 text-sm leading-7 text-[#5d7788]">รายงาน CSV/XLSX และ Template Smart อสม. จะถูกสร้างผ่าน Apps Script และเก็บใน Drive เมื่อเปิดใช้ Export Job ใน environment นี้</p><div className="mt-5 flex flex-wrap gap-3"><Button className="primary-button" onClick={() => toast.info("Export จะเปิดใช้หลังตั้งค่า Template และสิทธิ์ Drive")}>ดูตัวอย่างรายงาน</Button><Button variant="outline" onClick={() => toast.info("รายงานจะแสดงเฉพาะข้อมูลตามสิทธิ์")}>นโยบายการแสดงผล</Button></div></section></div></main></div>;
}

function Brand() { return <div className="brand-lockup"><img src="/manus-storage/kcg-mark_7b65e726.png" alt="KCG Health OSM" className="brand-mark" /><div><div className="brand-name">KCG</div><div className="brand-sub">HEALTH OSM</div></div></div>; }

function PublicPortal({ onBack }: { onBack: () => void }) {
  const [publicData, setPublicData] = useState<{ announcements?: Array<{ title?: string; summary?: string }>; emergencyPhone?: string; serviceHours?: string } | null>(null);
  useEffect(() => { void apiRequest<typeof publicData>("public.home").then((response) => setPublicData(response.data || null)).catch(() => undefined); }, []);
  return <div className="public-page"><header className="public-topbar"><Brand /><button className="public-back" onClick={onBack}><LogIn size={16} /> สำหรับเจ้าหน้าที่</button></header><main className="public-content"><section className="public-hero"><div className="public-hero-copy"><div className="eyebrow"><span className="sun-line" /> หน่วยบริการปฐมภูมิใกล้บ้าน</div><h1>สุขภาพชุมชน<br /><em>เริ่มต้นที่โคกชะงาย</em></h1><p>ข่าวสาร ตารางบริการ และช่องทางติดต่อจาก รพ.สต.โคกชะงาย เปิดดูได้ทันทีโดยไม่ต้องติดตั้งแอป</p><div className="public-hero-actions"><a className="primary-button" href="tel:074000000"><Phone size={17} /> โทรติดต่อหน่วยบริการ</a><button className="secondary-action" onClick={() => toast.info("เปิดให้บริการวันจันทร์–ศุกร์ 08:30–16:30 น.")}>ดูตารางบริการ <ChevronRight size={15} /></button></div></div><div className="hero-image-wrap"><img src="/manus-storage/kcg-community-hero_d89be46d.jpg" alt="เจ้าหน้าที่และ อสม. ในชุมชน" /><div className="hero-caption"><MapPin size={14} /> ตำบลโคกชะงาย · จังหวัดพัทลุง</div></div></section><section className="public-cards"><article className="public-card feature"><div className="card-label">{publicData?.announcements?.length ? "ประกาศล่าสุด" : "ข้อมูลบริการ · Staging"}</div><h2>{publicData?.announcements?.[0]?.title || "บริการสุขภาพใกล้บ้าน"}</h2><p>{publicData?.announcements?.[0]?.summary || "ข้อมูลสาธารณะจาก รพ.สต.โคกชะงาย ไม่แสดงข้อมูลสุขภาพรายบุคคล"}</p><button onClick={() => toast.info("ประกาศกิจกรรม", { description: "กรุณาติดตามประกาศจากหน่วยบริการ" })}>อ่านรายละเอียด <ArrowUpRight size={15} /></button></article><article className="public-card"><div className="public-card-icon yellow"><CalendarDays size={21} /></div><div className="card-label">ตารางบริการ</div><h3>ออกหน่วยใกล้บ้าน</h3><p>ดูวัน เวลา และพื้นที่ให้บริการ</p><button onClick={() => toast.info("กำลังเตรียมตารางบริการ")}>ดูตาราง <ChevronRight size={15} /></button></article><article className="public-card"><div className="public-card-icon blue"><BookOpen size={21} /></div><div className="card-label">ความรู้สุขภาพ</div><h3>ดูแลตัวเองได้ทุกวัน</h3><p>เนื้อหาที่ผ่านการตรวจทาน</p><button onClick={() => toast.info("กำลังเตรียมคลังความรู้")}>เปิดคลังความรู้ <ChevronRight size={15} /></button></article></section><section className="emergency-banner"><div className="emergency-icon"><Siren size={22} /></div><div><strong>กรณีฉุกเฉิน โทร {publicData?.emergencyPhone || "1669"}</strong><span>เว็บนี้ไม่ใช่บริการฉุกเฉิน</span></div><a href={`tel:${publicData?.emergencyPhone || "1669"}`}><Phone size={17} /> โทร {publicData?.emergencyPhone || "1669"}</a></section></main><footer className="public-footer"><span>รพ.สต.โคกชะงาย · ตำบลโคกชะงาย จังหวัดพัทลุง</span><span>ระบบสาธารณะไม่แสดงข้อมูลสุขภาพรายบุคคล</span></footer></div>; }
